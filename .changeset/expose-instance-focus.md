---
"@gpuix/native": minor
"@gpuix/react": minor
---

Expose `focus()` and `blur()` on host element refs so shared React components can use the standard HTMLElement-shaped imperative focus API.

Fixes #218
