---
'@gpuix/native': patch
'@gpuix/react': patch
---

Reconcile the fork with upstream 0.5.1, including text highlighting, expanded desktop automation, the bare code surface, Windows GPU test rendering, and the documentation site.

Preserve the fork's subscribed window sizing, phased wheel automation, hardened virtual-list estimates, strict diagnostics, per-renderer test state, and frame-pacing guarantees.
