---
'@gpuix/native': minor
'@gpuix/react': minor
---

Add `style.fontVariantNumeric`, the CSS `font-variant-numeric` property. `tabular-nums` gives every digit the same advance so figure columns align; the other values map to their OpenType features the same way. Inherited, as in CSS.
