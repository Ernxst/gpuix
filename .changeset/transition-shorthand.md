---
'@gpuix/native': minor
'@gpuix/react': minor
---

Add CSS transition shorthand strings with per-property timing and clocks to the
native and React renderers. Shorthand properties use kebab-case CSS names and
support named and cubic-bezier easings.

Fixes #454
