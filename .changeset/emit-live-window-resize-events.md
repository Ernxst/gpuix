---
"@gpuix/native": patch
"@gpuix/react": patch
---

Emit live logical window dimensions and scale factor on native resizes, and make `useWindowSize()` update mounted React consumers through a shared subscription.
