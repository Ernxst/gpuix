---
'@gpuix/native': major
'@gpuix/react': major
---

Remove the integer grid template shorthand and the `gridColumnMin` and `gridRowMin` properties.
Use an explicit CSS Grid track list for `gridTemplateColumns` and `gridTemplateRows` instead.

Fixes #280
