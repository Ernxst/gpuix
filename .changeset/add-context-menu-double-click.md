---
"@gpuix/native": minor
"@gpuix/react": minor
---

Add `onDoubleClick` and `onContextMenu` events with DOM ordering, click detail, and cancellation semantics.

Both fire on `<canvas>`, `<img>`, `<svg>`, `<code>`, `<diff>`, `<markdown>` and `<anchored>` as well as on host containers.

Fixes #224
