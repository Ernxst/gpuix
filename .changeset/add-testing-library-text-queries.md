---
"@gpuix/react": minor
---

Add Testing Library-style text queries to `@gpuix/react/testing`: `getByText`,
`queryByText`, `getAllByText`, and scoped `within`, with element-table helpers
and readable nearby-text failures.

Fixes #30
