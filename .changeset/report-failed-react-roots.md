---
"@gpuix/react": patch
---

Expose uncaught React root failures as inspectable root state and renderer diagnostics, and terminate top-level apps instead of leaving a silently dead native window.

Fixes #138
