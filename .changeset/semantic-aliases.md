---
'@gpuix/react': patch
---

Restore semantic role, focus, and keyboard activation behaviour for `<button>`
and `<a>` aliases, and reject invalid whole-prop style values with actionable
developer diagnostics.

Fixes #17
