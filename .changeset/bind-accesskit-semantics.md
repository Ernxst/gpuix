---
"@gpuix/native": minor
"@gpuix/react": minor
---

Bind explicit React accessibility roles, names, descriptions, states, values,
focus, and actions to GPUI's native AccessKit tree. GPU-backed tests can inspect
the real last-drawn tree and inject Activate, Increment, Decrement, and Focus
requests through GPUI's production action route.

Fixes #22
