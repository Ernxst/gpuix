---
"@gpuix/native": minor
"@gpuix/react": minor
---

Validate styles field-by-field with actionable element, property, and value diagnostics; support named colors, multi-stop linear-gradient backgrounds, `letterSpacing`, `textTransform`, and `textWrap`. Invalid style fields remain recoverable, while rejected mutation envelopes and renderer lifecycle failures stay fatal.

Fixes #3
