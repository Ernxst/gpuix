{
  "name": "@gpuix/example-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "bun build --compile --production app.tsx --outfile dist/todo",
    "web:dev": "bun web.ts",
    "screenshot": "bun screenshot.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gpuix/react": "workspace:^",
    "react": "^19.2.4"
  },
  "devDependencies": {
    "@gpuix/plugins": "workspace:*",
    "@types/bun": "^1.2.0",
    "@types/react": "^19.2.0",
    "typescript": "^5.3.0",
    "vite": "^8.2.1",
    "vitest": "^4.0.18"
  }
}
