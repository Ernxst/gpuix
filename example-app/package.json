{
  "name": "@gpuix/example-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "bun build --compile app.tsx --outfile dist/todo",
    "web:dev": "bun web.ts",
    "screenshot": "bun screenshot.ts",
    "test": "node ./node_modules/vitest/vitest.mjs run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gpuix/react": "workspace:^",
    "react": "^19.2.4"
  },
  "devDependencies": {
    "@gpuix/vite": "workspace:*",
    "@types/bun": "^1.2.0",
    "@types/react": "^19.2.0",
    "typescript": "^5.3.0",
    "vite": "^8.2.1",
    "vitest": "^4.0.18"
  }
}
